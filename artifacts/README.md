# artifacts

Use this directory for local non-git artifacts that should persist across experiments, such as checkpoints or exported model bundles. Every artifact should have a nearby JSON metadata file recording the git commit, config path, data split, and target generation settings.
